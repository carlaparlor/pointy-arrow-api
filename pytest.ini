[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --tb=short -p no:cacheprovider
markers =
    live: exercises the real gratisfy.xyz / auth.gratisfy.xyz / api.mail.tm endpoints (needs unrestricted egress)
filterwarnings =
    ignore::DeprecationWarning
